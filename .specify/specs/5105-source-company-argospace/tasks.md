# Tasks: Spec 5105 — source-company-argospace

- [x] T1 — Scaffold `packages/plugins/source-company-argospace/` package with package.json, tsconfig.json, and src barrel.
- [x] T2 — Implement `ArgospaceService` to parse the visible grouped careers list, follow detail pages, and emit `JobPostDto`.
- [x] T3 — Register `ArgospaceModule` in `site.enum.ts`, `packages/plugins/index.ts`, `tsconfig.base.json`, and `jest.config.js`.
- [x] T4 — Add `__tests__/fixtures/argospace-careers.html` and detail-page fixtures; write `__tests__/argospace.service.spec.ts`.
- [x] T5 — Update `docs/index.md` and `docs/log.md`.
- [x] T6 — Run typecheck and focused jest tests.
- [x] T7 — Commit, push, and open PR.
